type Coin {
  # Example field (placeholder)
  exampleField: Int
}

input CreateCoinInput {
  # Example field (placeholder)
  exampleField: Int
}

type Query {
  coins: [Coin]!
  coin(id: Int!): Coin
}

type Mutation {
  createCoin(createCoinInput: CreateCoinInput!): Coin!
}
